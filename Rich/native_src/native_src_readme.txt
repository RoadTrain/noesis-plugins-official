All source in here should be compiled as placed in the root directory structure of the pluginsource.zip file that is packaged with the latest Noesis distribution.

Meaning, when compiling these plugins, pluginshare.h should be in the same directory as this file relative to the plugin source code.
